// metro.config.js
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

// (optional) keep it vanilla; no extra customizations needed
module.exports = config;
